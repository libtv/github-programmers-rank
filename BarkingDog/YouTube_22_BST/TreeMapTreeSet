TreeSet은 Java의 컬렉션 인터페이스인 Set 인터페이스를 구현한 클래스입니다. TreeSet은 이진 검색 트리(Binary Search Tree)를 기반으로 구현되어 있으며, 원소를 자동으로 정렬된 상태로 유지합니다. TreeSet에서 제공하는 주요 메서드를 알려드리겠습니다.

add(E element): TreeSet에 원소를 추가합니다.
remove(Object element): TreeSet에서 주어진 원소를 제거합니다.
contains(Object element): TreeSet에 주어진 원소가 포함되어 있는지 여부를 확인합니다.
size(): TreeSet의 원소 개수를 반환합니다.
isEmpty(): TreeSet이 비어 있는지 여부를 확인합니다.
clear(): TreeSet의 모든 원소를 제거하여 비웁니다.
first(): TreeSet에서 가장 작은(정렬 순서상 가장 앞에 있는) 원소를 반환합니다.
last(): TreeSet에서 가장 큰(정렬 순서상 가장 뒤에 있는) 원소를 반환합니다.
higher(E element): 주어진 원소보다 큰 가장 작은 원소를 반환합니다.
lower(E element): 주어진 원소보다 작은 가장 큰 원소를 반환합니다.
ceiling(E element): 주어진 원소와 동일한 원소가 있으면 해당 원소를 반환하고, 없으면 주어진 원소보다 큰 가장 작은 원소를 반환합니다.
floor(E element): 주어진 원소와 동일한 원소가 있으면 해당 원소를 반환하고, 없으면 주어진 원소보다 작은 가장 큰 원소를 반환합니다.
iterator(): TreeSet의 원소를 순회하는 Iterator를 반환합니다.
위의 메서드들을 활용하여 TreeSet을 다양한 방식으로 활용할 수 있습니다. TreeSet은 원소의 자동 정렬과 검색에 효율적인 기능을 제공하기 때문에 유용하게 사용할 수 있는 자료구조입니다.

TreeMap은 Java의 컬렉션 인터페이스인 Map 인터페이스를 구현한 클래스입니다. TreeMap은 이진 검색 트리(Binary Search Tree)를 기반으로 구현되어 있으며, 키-값 쌍을 자동으로 정렬된 상태로 유지합니다. TreeMap에서 제공하는 주요 메서드를 알려드리겠습니다.

put(K key, V value): TreeMap에 주어진 키와 값을 추가합니다.
get(Object key): 주어진 키에 해당하는 값을 반환합니다.
remove(Object key): 주어진 키에 해당하는 키-값 쌍을 제거합니다.
containsKey(Object key): TreeMap에 주어진 키가 포함되어 있는지 여부를 확인합니다.
containsValue(Object value): TreeMap에 주어진 값이 포함되어 있는지 여부를 확인합니다.
size(): TreeMap의 키-값 쌍 개수를 반환합니다.
isEmpty(): TreeMap이 비어 있는지 여부를 확인합니다.
clear(): TreeMap의 모든 키-값 쌍을 제거하여 비웁니다.
firstKey(): TreeMap에서 가장 작은(정렬 순서상 가장 앞에 있는) 키를 반환합니다.
lastKey(): TreeMap에서 가장 큰(정렬 순서상 가장 뒤에 있는) 키를 반환합니다.
higherKey(K key): 주어진 키보다 큰 가장 작은 키를 반환합니다.
lowerKey(K key): 주어진 키보다 작은 가장 큰 키를 반환합니다.
ceilingKey(K key): 주어진 키와 동일한 키가 있으면 해당 키를 반환하고, 없으면 주어진 키보다 큰 가장 작은 키를 반환합니다.
floorKey(K key): 주어진 키와 동일한 키가 있으면 해당 키를 반환하고, 없으면 주어진 키보다 작은 가장 큰 키를 반환합니다.
keySet(): TreeMap의 모든 키를 포함하는 Set을 반환합니다.
values(): TreeMap의 모든 값들을 포함하는 Collection을 반환합니다.
entrySet(): TreeMap의 모든 키-값 쌍을 포함하는 Set을 반환합니다.
iterator(): TreeMap의 키-값 쌍을 순회하는 Iterator를 반환합니다.
위의 메서드들을 활용하여 TreeMap을 다양한 방식으로 활용할 수 있습니다. TreeMap은 키-값 쌍의 자동 정렬과 검색에 효율적인 기능을 제공하기 때문에 유용하게 사용할 수 있는 자료구조입니다.